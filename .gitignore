# Lambda deployment artifacts
lambda.zip
chat_lambda.zip
packaged.yaml

# Outputs locales del despliegue
agent.env
respuesta.json

# Python cache
__pycache__/

# Bundle temporal Lambda demo web (esbuild)
src-web/index.js

# npm: se instala en CloudShell/local con deploy.sh; no versionar (Windows: rutas muy largas)
node_modules/
*.pyc
*.pyo

# AWS SAM / local testing
.aws-sam/

# Environment files
.env
*.env
